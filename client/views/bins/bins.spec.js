'use strict';

describe('Controller: BinsCtrl', function () {

  beforeEach(module('partsTracker'));

  var BinsCtrl;

  beforeEach(inject(function ($controller) {
    BinsCtrl = $controller('BinsCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
